#include <stdio.h>
#include <math.h>

int main(int argc, const char *argv[]){

	double numero = 2.0;
	double resultado = sqrt(numero);
	printf("La raiz cuadrada de %f es: %f\n", numero, resultado);

	return 0;
}

